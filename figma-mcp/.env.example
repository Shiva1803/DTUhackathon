# ==============================================
# ENVIRONMENT VARIABLES - FRONTEND
# ==============================================
#
# SECURITY RULES:
# ✅ SAFE: API base URLs, feature flags, public keys
# ❌ NEVER: Secrets, private keys, database credentials
#
# WHY THIS STRUCTURE?
# - All frontend env vars MUST be prefixed with VITE_
# - Vite only exposes VITE_* vars to the client bundle
# - This prevents accidental secret leakage
# - Backend secrets live in server/.env (separate)
#
# HACKATHON SAFETY:
# - Use DEMO_MODE to enable fallback data
# - Prevents rate limiting during live demos
# - Graceful degradation if APIs are down
# ==============================================

# API Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# Environment
VITE_ENV=development

# Feature Flags
VITE_ENABLE_DEMO_MODE=true
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_VOICE_NARRATION=true

# Demo Safety
# When true, uses mock data instead of real API calls
VITE_USE_MOCK_DATA=false

# Public Keys (if needed for client-side integrations)
# VITE_PUBLIC_STRIPE_KEY=pk_test_...
# VITE_PUBLIC_ANALYTICS_ID=UA-...

# ==============================================
# WHAT NEVER GOES HERE:
# ❌ Database credentials
# ❌ API secrets/private keys
# ❌ OAuth client secrets
# ❌ JWT signing keys
# ❌ Any server-side tokens
#
# Those belong in: server/.env (backend only)
# ==============================================
