/**
 * PostCSS Configuration
 * 
 * Minimal, production-safe setup for:
 * - Tailwind CSS processing
 * - Autoprefixer for cross-browser compatibility
 */

export default {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
};
